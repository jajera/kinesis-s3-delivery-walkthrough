# Prefer the user-local AWS CLI pin (2.36.38) over an older system install.
export PATH="$HOME/.local/bin:$PATH"
export AWS_PROFILE="${AWS_PROFILE:-sandbox}"
export AWS_REGION="${AWS_REGION:-ap-southeast-2}"
